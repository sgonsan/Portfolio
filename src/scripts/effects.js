// =======================
// Frontend effects
// Currently empty — the brutalist redesign relies purely on CSS
// transitions for hover states, so no extra JS animations are needed.
// Kept as a no-op stub to preserve the public init API in case future
// effects are reintroduced.
// =======================
export function initEffects() {}
